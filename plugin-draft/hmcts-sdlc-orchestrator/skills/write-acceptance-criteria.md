# Skill: Write Acceptance Criteria

This skill has moved to the [agentic-plugins-marketplace](https://github.com/hmcts/agentic-plugins-marketplace) as part of the `bdd-workflow` plugin (bundled with `generate-bdd-specs`).

Install with:

```
/plugin install bdd-workflow@agentic-plugins-marketplace
```

No HMCTS overlay is needed — the public version is used as-is.
